name: test_env
channels:
    - defaults
dependencies:
    - python=3.5
    - numpy
    - scipy
    - networkx
    - pandas
    - matplotlib
    - plotly
    - nose
    - openpyxl
    - pip:
        - coveralls
